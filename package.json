{
  "name": "nellyx-default",
  "version": "1.0.0",
  "description": "Nellyx service default template — Node.js runtime",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "health": "curl -sf http://localhost:${PORT:-3000}/health"
  },
  "engines": {
    "node": ">=18"
  }
}
